# Add to admin.simfree.io server block in /etc/nginx/sites-available/simfree

upstream simfree_video {
    server 127.0.0.1:3004;
    keepalive 16;
}

# Inside server { server_name admin.simfree.io; ... }
#   location /video-api/ {
#       proxy_pass http://simfree_video/;
#   }
